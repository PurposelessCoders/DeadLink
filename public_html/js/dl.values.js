/**
 * @namespace tsc
 */

(function () {
  /**
   * @namespace values
   */
  dl.values = {};
 
    /**
   * @constant
   */
  dl.values.FRAME_RATE = 60;

  dl.values.HEIGHT = 400;
  dl.values.WIDTH = 500;
}).call(this);

